import 'package:get/get.dart';

class Dimensions {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;

  //screenHeigt=759.0 / the item normal size size
  //screenWidth=360.0

  static double pageView = screenHeight / 2.64;
  static double pageViewContainer = screenHeight / 3.84;
  static double pageViewTextContainer = screenHeight / 7.03;

  //############  Start  Size  ############//
  static double size5 = screenWidth / (screenWidth / 5);
  static double size10 = screenWidth / (screenWidth / 10);
  static double size15 = screenWidth / (screenWidth / 15);
  static double size20 = screenWidth / (screenWidth / 20);
  static double size25 = screenWidth / (screenWidth / 25);
  static double size30 = screenWidth / (screenWidth / 30);
  static double size35 = screenWidth / (screenWidth / 35);
  static double size40 = screenWidth / (screenWidth / 40);
  static double size45 = screenWidth / (screenWidth / 45);
  static double size50 = screenWidth / (screenWidth / 50);

//############  End  Size  ############//

//############  Start Width Size  ############//
  static double width2 = screenWidth / (screenWidth / 2);
  static double width5 = screenWidth / (screenWidth / 5);
  static double width10 = screenWidth / (screenWidth / 10);
  static double width15 = screenWidth / (screenWidth / 15);
  static double width20 = screenWidth / (screenWidth / 20);
  static double width25 = screenWidth / (screenWidth / 25);
  static double width30 = screenWidth / (screenWidth / 30);
  static double width35 = screenWidth / (screenWidth / 35);
  static double width40 = screenWidth / (screenWidth / 40);
  static double width45 = screenWidth / (screenWidth / 45);
  static double width50 = screenWidth / (screenWidth / 50);
  static double width75 = screenWidth / (screenWidth / 75);
  static double width100 = screenWidth / (screenWidth / 100);
  static double width120 = screenWidth / (screenWidth / 120);

//############  End Width Size  ############//

//############  Start Height Size  ############//
  static double height5 = screenHeight / (screenHeight / 5);
  static double height10 = screenHeight / (screenHeight / 10);
  static double height15 = screenHeight / (screenHeight / 15);
  static double height20 = screenHeight / (screenHeight / 20);
  static double height25 = screenHeight / (screenHeight / 25);
  static double height30 = screenHeight / (screenHeight / 30);
  static double height35 = screenHeight / (screenHeight / 35);
  static double height40 = screenHeight / (screenHeight / 40);
  static double height45 = screenHeight / (screenHeight / 45);
  static double height50 = screenHeight / (screenHeight / 50);
  static double height75 = screenHeight / (screenHeight / 75);
  static double height100 = screenHeight / (screenHeight / 100);
  static double height120 = screenHeight / (screenHeight / 120);
//############  Start Height Size  ############//

//############  Start font Size  ############//
  static double font5 = screenHeight / (screenHeight / 5);
  static double font10 = screenHeight / (screenHeight / 10);
  static double font15 = screenHeight / (screenHeight / 15);
  static double font20 = screenHeight / (screenHeight / 20);
  static double font25 = screenHeight / (screenHeight / 25);
  static double font30 = screenHeight / (screenHeight / 30);
  static double font35 = screenHeight / (screenHeight / 35);
  static double font40 = screenHeight / (screenHeight / 40);
  static double font45 = screenHeight / (screenHeight / 45);
  static double font50 = screenHeight / (screenHeight / 50);
//############  Start font Size  ############//

//############  Start Radius Size  ############//
  static double radius5 = screenHeight / (screenHeight / 5);
  static double radius10 = screenHeight / (screenHeight / 10);
  static double radius15 = screenHeight / (screenHeight / 15);
  static double radius20 = screenHeight / (screenHeight / 20);
  static double radius25 = screenHeight / (screenHeight / 25);
  static double radius30 = screenHeight / (screenHeight / 30);
  static double radius35 = screenHeight / (screenHeight / 35);
  static double radius40 = screenHeight / (screenHeight / 40);
  static double radius45 = screenHeight / (screenHeight / 45);
  static double radius50 = screenHeight / (screenHeight / 50);
//############  Start Radius Size  ############//

}
